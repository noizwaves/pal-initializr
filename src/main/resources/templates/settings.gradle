rootProject.name = '{{artifactId}}'

include "applications:{{artifactId}}-server"
{{#isWeb}}
include "components:{{artifactId}}-library"
{{/isWeb}}
{{#hasFlyway}}
include "databases:{{artifactId}}-database"
{{/hasFlyway}}
